Team Assignments Issue Debug Notes:

The issue: When selecting a Lead Manager from the dropdown, it only allows one selection then the dropdown disappears.

From the screenshot, I can see:
- Kyle Barks is the Acquisition Manager
- The dropdown shows "+ Add Lead Manager" 
- After selecting Chris Segura, the dropdown disappears instead of allowing more selections

Looking at the code:
1. The Select component has key={`am-${am.teamMemberId}-${assignedLeadManagers.length}`}
2. This should force a re-render when assignments change
3. The filter logic checks: isLeadManager && notAssigned

The problem might be:
1. The mutation is not invalidating the queries properly
2. The filter logic is excluding all lead managers after one is assigned
3. The teamMemberId linking might be wrong

From the database:
- team_assignments has 2 rows
- team_members has 3 rows (Daniel Lozano, Kyle Barks, Chris Segura)
- 4 users have lead_manager or acquisition_manager roles

The issue is likely that after assigning one LM, the dropdown shows empty because:
- Either all LMs are already assigned
- Or the filter logic is wrong

Need to check if there are multiple Lead Managers available to assign.
